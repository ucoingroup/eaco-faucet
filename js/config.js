/* EACO Faucet - Frontend Configuration
 * 
 * Set EACO_BACKEND to your PHP server URL for live claims.
 * Leave as null for demo mode (GitHub Pages deployment).
 * 
 * Example:
 *   window.EACO_BACKEND = "https://your-server.com/server";
 *   window.EACO_BACKEND = null;  // Demo mode
 */
window.EACO_BACKEND = null; // Set to your backend URL for live mode
